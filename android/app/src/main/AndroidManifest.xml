

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

<uses-permission android:name="android.permission.INTERNET"/>
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

<!-- سياسة Google Play لأذونات الصور والفيديو (Android 13+، API 33+): يُمنع
     طلب READ_MEDIA_IMAGES / READ_MEDIA_VIDEO ما دامت أدوات الاختيار المضمّنة
     بالنظام كافية. التطبيق يختار الصور حصراً عبر image_picker (أداة اختيار
     الصور من Android — Photo Picker، مُفعَّلة في main.dart) و file_picker
     (SAF)، وكلاهما يعيد URI بصلاحية مؤقتة دون أي إذن تخزين. نمنع صراحةً حقن
     أي إذن وسائط واسع من أي حزمة عبر manifest merge حتى لا يُرفض الإصدار
     مستقبلاً — tools:node="remove" يُزيلها من الـ manifest المدموج النهائي
     فلا تصل لفحص Play إطلاقاً (نفس أسلوب أذونات الكاميرا أدناه). -->
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove"/>
<uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove"/>
<uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" tools:node="remove"/>
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove"/>

<!-- حزمة camera (مُستورَدة فقط في lib/shared/widgets/qr_code.dart — شاشة
     ماسح QR غير مُستخدَمة إطلاقاً، كل كود المسح فيها معلَّق) تحقن تلقائياً
     CAMERA + RECORD_AUDIO + WRITE_EXTERNAL_STORAGE + ميزات الكاميرا في
     الـ manifest المدموج. التطبيق لا يلتقط بالكاميرا ولا يسجّل صوتاً (كل
     اختيار الصور من المعرض عبر image_picker الذي لا يحتاج أي إذن)، فتُزال
     من النسخة المنشورة لتفادي رفض المتاجر على أذونات بلا ميزة مقابلة.
     عند إضافة ميزة كاميرا حقيقية مستقبلاً: احذف أسطر tools:node أدناه
     وأعد إعلان CAMERA صراحةً. -->
<uses-permission android:name="android.permission.CAMERA" tools:node="remove"/>
<uses-permission android:name="android.permission.RECORD_AUDIO" tools:node="remove"/>
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove"/>
<uses-feature android:name="android.hardware.camera" tools:node="remove"/>
<uses-feature android:name="android.hardware.camera.any" tools:node="remove"/>
<uses-feature android:name="android.hardware.camera.autofocus" tools:node="remove"/>


<application

    android:label="أبعاد"
    android:name="${applicationName}"
    android:icon="@mipmap/ic_launcher"
    android:networkSecurityConfig="@xml/network_security_config"
    >

    <!-- Google Maps Key -->
    <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="AIzaSyBnNPMhqZIEVQ1WrCAaVHiS6PK1-gyf18g"/>

    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:launchMode="singleTop"
        android:taskAffinity=""
        android:theme="@style/LaunchTheme"
        android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
        android:hardwareAccelerated="true"
        android:windowSoftInputMode="adjustResize">

        <!-- ChottuLink يعالج نيّة رابط abaadapp.chottu.link بنفسه؛ نعطّل توجيه
             Flutter المدمج (الافتراضي أصلًا false، التطبيق يستخدم app_links
             + GetX) حسب توصية ChottuLink. -->
        <meta-data
            android:name="flutter_deeplinking_enabled"
            android:value="false"/>

        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>

        <!-- رابط الإحالة الفعلي abaadapp.sa/ref/CODE (App Links). assetlinks.json
             على الباكند يحمل حاليًا بصمة مفتاح debug.keystore (نفسه المستخدم
             مؤقتًا لبناء release أيضًا — انظر build.gradle.kts). بعد اعتماد
             مفتاح release حقيقي عبر key.properties، يجب إضافة بصمته الجديدة
             لقائمة ANDROID_SHA256_FINGERPRINTS في .env على السيرفر الفعلي. -->
        <intent-filter android:autoVerify="true">
            <action android:name="android.intent.action.VIEW"/>
            <category android:name="android.intent.category.DEFAULT"/>
            <category android:name="android.intent.category.BROWSABLE"/>
            <data android:scheme="https" android:host="abaadapp.sa" android:pathPrefix="/ref"/>
        </intent-filter>
        <!-- رابط تفاصيل العقار الفعلي app.abaadapp.sa/details/{id} -->
        <intent-filter android:autoVerify="true">
            <action android:name="android.intent.action.VIEW"/>
            <category android:name="android.intent.category.DEFAULT"/>
            <category android:name="android.intent.category.BROWSABLE"/>
            <data android:scheme="https" android:host="app.abaadapp.sa" android:pathPrefix="/details"/>
        </intent-filter>
        <!-- رابط الإحالة القصير من ChottuLink (abaadapp.chottu.link/xxxxx).
             ملفّا assetlinks.json و apple-app-site-association لهذا النطاق
             يستضيفهما ChottuLink تلقائيًا، ويجب أن يحملا اسم الحزمة
             sa.pdm.abaad.abaad وبصمات SHA-256 المسجَّلة في لوحة ChottuLink
             (Settings → Platforms → Android). -->
        <intent-filter android:autoVerify="true">
            <action android:name="android.intent.action.VIEW"/>
            <category android:name="android.intent.category.DEFAULT"/>
            <category android:name="android.intent.category.BROWSABLE"/>
            <data android:scheme="https" android:host="abaadapp.chottu.link"/>
        </intent-filter>

        <meta-data
            android:name="io.flutter.embedding.android.NormalTheme"
            android:resource="@style/NormalTheme"/>

    </activity>

    <meta-data
        android:name="flutterEmbedding"
        android:value="2"/>

    <!-- إعدادات FCM الافتراضية للإشعارات (تُستخدَم عندما يكون التطبيق في
         الخلفية ويعرض النظام الإشعار تلقائيًا بدون المرور بكود Dart). -->
    <meta-data
        android:name="com.google.firebase.messaging.default_notification_channel_id"
        android:value="abaad_notifications"/>
    <meta-data
        android:name="com.google.firebase.messaging.default_notification_icon"
        android:resource="@drawable/ic_notification"/>
</application>

</manifest>
